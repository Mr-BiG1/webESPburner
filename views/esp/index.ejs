<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Ledger Transactions</h1>
        <a href="/transactions/add/new" class="btn btn-primary">
            <i class="fas fa-plus"></i> Add Transaction
        </a>
    </div>
    <% if (errorMessage) { %>
        <div class="alert alert-danger">
            <%= errorMessage %>
        </div>
    <% } %>

    <body>
        <h1>ESP Web Flasher</h1>
        <button id="connect">Connect to ESP</button>
        <input type="file" id="firmware" accept=".bin">
        <button id="flash">Flash Firmware</button>
        <pre id="log"></pre>
    
        <script src="script.js"></script>
    </body>
</div>